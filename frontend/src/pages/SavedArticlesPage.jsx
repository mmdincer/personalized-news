/**
 * Saved Articles Page
 * 
 * Protected route - displays all saved articles for the authenticated user
 * Uses SavedArticles component to display the list
 */

import SavedArticles from '../components/saved/SavedArticles';

const SavedArticlesPage = () => {
  return <SavedArticles />;
};

export default SavedArticlesPage;

